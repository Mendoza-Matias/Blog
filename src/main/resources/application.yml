spring:
  application:
    name: "blog"
  datasource:
    url: jdbc:postgresql://${HOST}:${POST}/${DB_NAME}
    username: ${USERNAME}
    password: ${PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: ${SERVER_PORT}
  servlet:
    context-path: "/api/v1"
